# mini-games

미니 게임 모음. **게임 하나 = 폴더 하나 = 독립 빌드.** 루트에 의존성을 두지 않는다.

| 폴더 | 게임 | 상태 |
|---|---|---|
| `dotori-basket/` | 도토리 숲 바구니 — 레시피 순서대로 과일 받기 | 골조 수립 중 |

각 게임의 규칙·불변식은 **그 폴더의 `CLAUDE.md`**에 있다. 작업 전에 반드시 읽는다.

## 🔴 커밋

전역 규칙(`~/.claude/docs/commit-rules.md`)은 모든 커밋에 Jira 티켓 `ICW-XXXXX:`를 요구하지만
**이 저장소에는 Jira가 없다.** 여기서는 아래를 따른다.

```
<게임 폴더명>: 한국어 요약        예) dotori-basket: 레시피 제한시간 30초 고정
repo: 한국어 요약                 루트 공통 변경
```

전역 `skill-usage-rules.md`가 커밋 시 `git-commit` 스킬 호출을 강제하고 그 스킬이 티켓을
요구하므로, **이 저장소에서는 `git-commit` 스킬을 쓰지 않고 `git commit`을 직접 호출한다.**

커밋 아이덴티티는 **저장소 단위**로 개인 계정이 설정돼 있다(전역은 회사 계정이므로 건드리지 않는다).
`git config user.email`이 개인 주소인지 확인하고 커밋한다.

## 🔴 에셋을 생성하지 않는다

SpriteCook 등 이미지 생성 도구를 호출하지 않는다. 새 에셋은 **기존 생성물에서 실측한 수치로
벡터(SVG)를 직접 작업**한다 — 이 방식으로 만든 수박·배·체리·돌멩이·나뭇가지가 생성물과 같은
세트로 보이고, 방해물은 과일과 실루엣이 겹치지 않는다.
절차는 `dotori-basket/docs/art/pipeline.md`.

## 언어

문서와 코드 주석은 한국어로 쓴다. 기존 코드가 전부 그렇다.

## 게임을 추가할 때

폴더 하나를 만들고 그 안에 독립 `package.json`을 둔다. 위 표에 한 줄 추가한다.
